import React from "react";
import "../styles/App.css";

const Navbar = () => (
  <nav className="navbar">
    <div className="navbar-title">Controle de Alunos</div>
    <div className="navbar-links">
      <a href="/">Início</a>
    </div>
  </nav>
);

export default Navbar;
